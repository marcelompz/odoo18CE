<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Template completamente nuevo para agregar campo de comprobante de transferencia -->
    <template id="payment_transfer_receipt_section_new" name="Payment Transfer Receipt Section">
        <div class="oe_structure clearfix mt-3 mb-4">
            <div class="row">
                <div class="col-12">
                    <div class="card">
                        <div class="card-header">
                            <h5 class="mb-0">
                                <i class="fa fa-receipt me-2"></i>
                                Comprobante de Transferencia (Opcional)
                            </h5>
                        </div>
                        <div class="card-body">
                            <div class="alert alert-info">
                                <i class="fa fa-info-circle me-2"></i>
                                <strong>Importante:</strong> Si selecciona transferencia bancaria, puede subir el comprobante de pago aquí. 
                                El archivo se guardará como comentario en su pedido antes de la confirmación.
                            </div>
                            <div class="mb-3">
                                <label for="transfer_receipt_file" class="form-label">
                                    Subir comprobante de transferencia
                                </label>
                                <input type="file" 
                                       class="form-control" 
                                       id="transfer_receipt_file" 
                                       name="bank_transfer_receipt" 
                                       accept=".pdf,.jpg,.jpeg,.png,.doc,.docx,.txt"/>
                                <div class="form-text">
                                    Formatos permitidos: PDF, JPG, PNG, DOC, DOCX, TXT. Tamaño máximo: 10MB
                                </div>
                                <div id="transfer_file_info" class="mt-2" style="display: none;">
                                    <div class="alert alert-success py-2">
                                        <i class="fa fa-check-circle me-2"></i>
                                        <span id="transfer_file_name"></span>
                                        <small class="d-block text-muted" id="transfer_file_size"></small>
                                    </div>
                                </div>
                            </div>
                            
                            <!-- Script para asegurar que el formulario tenga enctype -->
                            <script type="text/javascript">
                                document.addEventListener('DOMContentLoaded', function() {
                                    var fileInput = document.getElementById('transfer_receipt_file');
                                    
                                    if (fileInput) {
                                        // Asegurar que el formulario tenga enctype
                                        var form = fileInput.closest('form');
                                        if (form) {
                                            form.setAttribute('enctype', 'multipart/form-data');
                                        }
                                        
                                        // Agregar evento de cambio
                                        fileInput.addEventListener('change', function() {
                                            var file = this.files[0];
                                            if (file) {
                                                // Archivo seleccionado
                                            }
                                        });
                                    }
                                });
                            </script>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </template>

    <!-- Template para heredar y incluir solo el HTML directo del controlador -->
    <template id="payment_transfer_receipt_section" inherit_id="website_sale.payment">
        <xpath expr="//div[@id='payment_method']" position="after">
            <t t-if="transfer_receipt_html">
                <t t-raw="transfer_receipt_html"/>
            </t>
        </xpath>
    </template>
</odoo> 