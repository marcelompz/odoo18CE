<?xml version="1.0" encoding="utf-8"?>
<!-- 
    Create on 2026-02-03 11:23:33
    @author: drojo
-->
<odoo>

    <!-- tree view -->

    <record id="account_payment_inherit_tree" model="ir.ui.view">
        <field name="name">account.payment.inherit.tree</field>
        <field name="model">account.payment</field>
        <field name="inherit_id" ref="account.view_account_payment_tree"/>
        <field name="arch" type="xml">

            <xpath expr="//field[@name='state']" position="before">
                <field name="origin_bank_id" optional="hide"/>
                <field name="account_holder" optional="hide"/>
                <field name="operation_number" optional="hide"/>
                <field name="note" optional="hide"/>
            </xpath>
        
        </field>
    </record>

    <!-- form view -->

    <record id="account_payment_inherit_form" model="ir.ui.view">
        <field name="name">account.payment.inherit.form</field>
        <field name="model">account.payment</field>
        <field name="inherit_id" ref="account.view_account_payment_form" />
        <field name="arch" type="xml">

            <xpath expr="//field[@name='payment_method_line_id']" position="after">
                <field name="note" readonly="state != 'draft'"/>
            </xpath>

            <xpath expr="//field[@name='memo']" position="after">
                <field name="origin_bank_id" readonly="state != 'draft'"/>
                <field name="account_holder" readonly="state != 'draft'"/>
                <field name="operation_number" readonly="state != 'draft'"/>
            </xpath>
        
        </field>
    </record>

    <!-- search view -->
    <record id="account_payment_inherit_search" model="ir.ui.view">
        <field name="name">account.payment.inherit.search</field>
        <field name="model">account.payment</field>
        <field name="inherit_id" ref="account.view_account_payment_search" />
        <field name="arch" type="xml">
            
            <xpath expr="//search" position="inside">
                <field name="origin_bank_id"/>
                <field name="account_holder"/>
                <field name="operation_number"/>
            </xpath>
            
        </field>
    </record>

</odoo>
