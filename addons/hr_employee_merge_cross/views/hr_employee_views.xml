<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="server_action_hr_employee_merge" model="ir.actions.server">
        <field name="name">Fusionar empleados duplicados</field>
        <field name="model_id" ref="hr.model_hr_employee"/>
        <field name="binding_model_id" ref="hr.model_hr_employee"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">
if not records or len(records) &lt; 2:
    raise UserError(_('Seleccione 2 o mas empleados para fusionar.'))
action = {
    'type': 'ir.actions.act_window',
    'name': 'Fusionar empleados duplicados',
    'res_model': 'hr.employee.merge.wizard',
    'view_mode': 'form',
    'target': 'new',
    'context': {'active_ids': records.ids, 'active_model': 'hr.employee'},
}
        </field>
    </record>

</odoo>
