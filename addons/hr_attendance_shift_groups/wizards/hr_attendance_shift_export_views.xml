<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_hr_attendance_shift_export_form" model="ir.ui.view">
        <field name="name">hr.attendance.shift.export.form</field>
        <field name="model">hr.attendance.shift.export</field>
        <field name="arch" type="xml">
            <form string="Exportar Tablero a Excel">
                <group>
                    <group>
                        <field name="period_type"/>
                        <field name="date_from"/>
                        <field name="date_to"/>
                        <field name="include_empty_days"/>
                    </group>
                    <group>
                        <field name="shift_group_ids" widget="many2many_tags"/>
                        <field name="department_ids" widget="many2many_tags"/>
                        <field name="employee_ids" widget="many2many_tags"/>
                    </group>
                </group>
                <group invisible="not file_data">
                    <field name="file_name" readonly="1"/>
                    <field name="file_data" filename="file_name" readonly="1"/>
                </group>
                <p class="text-muted">
                    Genera un archivo .xlsx con la planilla por turnos. Se crea una hoja por
                    cada Grupo de Turno, con columnas dinámicas según las franjas configuradas.
                </p>
                <footer>
                    <button name="action_export" type="object" string="Generar Excel" class="btn-primary"/>
                    <button special="cancel" string="Cancelar" class="btn-secondary"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_hr_attendance_shift_export_wizard" model="ir.actions.act_window">
        <field name="name">Exportar Planilla Excel</field>
        <field name="res_model">hr.attendance.shift.export</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

</odoo>
