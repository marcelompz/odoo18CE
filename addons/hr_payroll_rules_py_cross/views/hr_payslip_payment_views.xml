<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Stat button "Pagos" en el header del recibo -->
    <record id="view_hr_payslip_form_inherit_payment" model="ir.ui.view">
        <field name="name">hr.payslip.form.payment.stat.cn</field>
        <field name="model">hr.payslip</field>
        <field name="inherit_id" ref="hr_payroll.view_hr_payslip_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_open_payments" type="object"
                        class="oe_stat_button" icon="fa-money"
                        invisible="payment_count == 0">
                    <field name="payment_count" widget="statinfo" string="Pagos"/>
                </button>
            </xpath>
        </field>
    </record>

</odoo>
