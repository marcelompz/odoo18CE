<odoo>
    <data>

        <record id="product_template_kanban_view_inherit" model="ir.ui.view">
            <field name="name">product.template.kanban.inherit</field>
            <field name="model">product.template</field>
            <field name="inherit_id" ref="product.product_template_kanban_view"/>
            <field name="arch" type="xml">
                <xpath expr="//kanban" position="attributes">
                    <attribute name="default_group_by">stage_id</attribute>
                    <attribute name="class">o_kanban_small_column</attribute>
                </xpath>
            </field>
        </record>

        <record id="product_template_view_form_add_stage" model="ir.ui.view">
            <field name="name">product.template.form.stage</field>
            <field name="model">product.template</field>
            <field name="inherit_id" ref="product.product_template_form_view"/>
            <field name="arch" type="xml">
                <xpath expr="//div[@name='options']" position="before">
                    <field name="stage_id" widget="statusbar" options="{'clickable': '1', 'fold_field': 'fold'}"/>
                </xpath>
            </field>
        </record>

        <record id="product_template_action_kanban" model="ir.actions.act_window">
            <field name="name">Kanban de Productos</field>
            <field name="res_model">product.template</field>
            <field name="view_mode">kanban</field>
            <field name="context">{}</field>
            <field name="domain">[]</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    Crear un nuevo producto
                </p>
            </field>
        </record>

        <!-- Menú para Kanban de Productos en el menú principal de Inventario -->
        <menuitem
            id="product_kanban_menu"
            name="Kanban de Productos"
            parent="stock.menu_stock_root"
            action="product_template_action_kanban"
            sequence="10"/>

    </data>
</odoo>

